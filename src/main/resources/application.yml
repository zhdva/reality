spring:
  main:
    allow-circular-references: true
server:
  port: 8080
app:
  host: "${APP_HOST}"
email:
  host: "${EMAIL_HOST}"
  port: "${EMAIL_PORT}"
  login: "${EMAIL_LOGIN}"
  password: "${EMAIL_PASSWORD}"
telegram:
  bot:
    name: "reality-bot"
    token: "${BOT_TOKEN}"
  chat:
    id: "${CHAT_ID}"